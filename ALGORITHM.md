# 📊 Алгоритм роботи Smart Climate Controller

## Параметри налаштування

Всі параметри налаштовуються через UI в розділі Configuration:

- `minor_hysteresis` = **0.5°C** - поріг для включення роботи (мінімальна різниця для реагування)
- `major_threshold` = **1.0°C** - поріг для вимикання (максимальне відхилення перед вимкненням)
- `minor_correction` = **5.0°C** - невелика корекція температури на кондиціонері
- `major_correction` = **10.0°C** - велика корекція температури на кондиціонері
- `use_linear_correction` = **False** - використання лінійної корекції температури (опціонально)

---

## 🎛️ Режими роботи

Система підтримує два режими корекції температури:

### 1. Стандартний режим (use_linear_correction = False)

В стандартному режимі використовуються фіксовані корекції:
- Без корекції (в діапазоні ±0.5°C)
- Велика корекція ±10°C (за межами ±0.5°C)

### 2. Лінійний режим (use_linear_correction = True)

В лінійному режимі корекція змінюється **пропорційно** до відхилення температури **тільки в діапазоні ±0.5°C**:
- В діапазоні ±0.5°C: корекція змінюється плавно від 0°C до ±5°C
- За межами ±0.5°C: використовується велика корекція ±10°C (як у стандартному режимі)
- Формула: `correction = (|temp_diff| / 0.5) × 5°C`

**Переваги лінійного режиму:**
- Більш плавне регулювання температури в діапазоні ±0.5°C
- Менші коливання температури біля цільового значення
- Краща точність підтримки цільової температури
- За межами ±0.5°C працює так само швидко, як стандартний режим

**Коли використовувати:**
- Лінійний режим: для приміщень, де важлива стабільність температури без коливань (плавна зміна корекції)
- Стандартний режим: для звичайного використання (чіткі пороги: норма або велика корекція)

---

## 🔥 Режим HEAT (Обігрів)

### Стандартний режим - Приклад: Цільова температура 22°C

| Поточна t°C | Різниця (temp_diff) | Дія | Температура на AC | Опис |
|-------------|---------------------|-----|-------------------|------|
| 24.5 | +2.5 | ❌ **ВИМКНУТИ** | OFF | Занадто тепло |
| 23.1 | +1.1 | ❌ **ВИМКНУТИ** | OFF | >1.0°C - вимикаємо |
| 22.8 | +0.8 | 🔥 **Велика корекція** | 12°C (22-10) | За межами +0.5°C |
| 22.5 | +0.5 | ✅ Працює | 22°C | На межі діапазону |
| 22.3 | +0.3 | ✅ Працює | 22°C | Норма - підтримка |
| 22.0 | 0.0 | ✅ Працює | 22°C | Ідеально |
| 21.7 | -0.3 | ✅ Працює | 22°C | Норма - підтримка |
| 21.5 | -0.5 | ✅ Працює | 22°C | На межі діапазону |
| 21.2 | -0.8 | 🔥 **Велика корекція** | 30°C (22+10) | За межами -0.5°C |
| 20.8 | -1.2 | 🔥 **Велика корекція** | 30°C (22+10) | <-1.0°C - дуже холодно |

### Лінійний режим - Приклад: Цільова температура 22°C

| Поточна t°C | Різниця (temp_diff) | Дія | Температура на AC | Розрахунок | Опис |
|-------------|---------------------|-----|-------------------|------------|------|
| 24.5 | +2.5 | ❌ **ВИМКНУТИ** | OFF | - | Занадто тепло |
| 23.1 | +1.1 | ❌ **ВИМКНУТИ** | OFF | - | >1.0°C - вимикаємо |
| 22.8 | +0.8 | 🔥 **Велика корекція** | 12°C | За межами ±0.5°C | Тепло, велика корекція |
| 22.5 | +0.5 | 📉 Лінійна корекція | 17°C | ratio=0.5/0.5=1.0, corr=-5°C | На межі лінійного діапазону |
| 22.3 | +0.3 | 📉 Лінійна корекція | 19°C | ratio=0.3/0.5=0.6, corr=-3°C | Трохи тепло |
| 22.0 | 0.0 | ✅ Працює | 22°C | Без корекції | Ідеально |
| 21.8 | -0.2 | 📈 Лінійна корекція | 24°C | ratio=0.2/0.5=0.4, corr=+2°C | Трохи холодно |
| 21.5 | -0.5 | 📈 Лінійна корекція | 27°C | ratio=0.5/0.5=1.0, corr=+5°C | На межі лінійного діапазону |
| 21.2 | -0.8 | 🔥 **Велика корекція** | 30°C | За межами ±0.5°C | Холодно, велика корекція |
| 20.8 | -1.2 | 🔥 **Велика корекція** | 30°C | <-1.0°C - мажорна | Дуже холодно |

### Логіка HEAT (Стандартний режим):

```
IF temp_diff > +1.0°C:
    → ВІМКНУТИ кондиціонер (дуже тепло)

ELSE IF temp_diff < -1.0°C:
    → Велика корекція: AC temp = target + 10°C (дуже холодно)

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    → Велика корекція: AC temp = target ± 10°C (за межами ±0.5°C)

ELSE (-0.5°C ≤ temp_diff ≤ +0.5°C):
    → Працює нормально: AC temp = target (комфортний діапазон)
```

### Логіка HEAT (Лінійний режим):

```
IF temp_diff > +1.0°C:
    → ВИМКНУТИ кондиціонер (дуже тепло)

ELSE IF temp_diff < -1.0°C:
    → Велика корекція: AC temp = target + 10°C (дуже холодно)

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    → Велика корекція: AC temp = target ± 10°C (за межами лінійного діапазону)

ELSE IF temp_diff в діапазоні [-0.5°C, +0.5°C]:
    → Лінійна корекція:
       ratio = |temp_diff| / 0.5°C             # 0.0 до 1.0

       IF temp_diff < 0 (холодніше):
           correction = ratio × 5°C             # 0°C до +5°C
           AC temp = target + correction

       ELSE IF temp_diff > 0 (тепліше):
           correction = ratio × 5°C             # 0°C до 5°C
           AC temp = target - correction        # віднімаємо

       ELSE (temp_diff = 0):
           AC temp = target                     # без корекції
```

**Діапазон роботи:** від -0.5°C до +1.0°C відносно цілі

---

## ❄️ Режим COOL (Охолодження)

### Стандартний режим - Приклад: Цільова температура 22°C

| Поточна t°C | Різниця (temp_diff) | Дія | Температура на AC | Опис |
|-------------|---------------------|-----|-------------------|------|
| 19.5 | -2.5 | ❌ **ВИМКНУТИ** | OFF | Занадто холодно |
| 20.9 | -1.1 | ❌ **ВИМКНУТИ** | OFF | <-1.0°C - вимикаємо |
| 21.2 | -0.8 | ❄️ **Велика корекція** | 30°C (22+10) | За межами -0.5°C |
| 21.5 | -0.5 | ✅ Працює | 22°C | На межі діапазону |
| 21.7 | -0.3 | ✅ Працює | 22°C | Норма - підтримка |
| 22.0 | 0.0 | ✅ Працює | 22°C | Ідеально |
| 22.3 | +0.3 | ✅ Працює | 22°C | Норма - підтримка |
| 22.5 | +0.5 | ✅ Працює | 22°C | На межі діапазону |
| 22.8 | +0.8 | ❄️ **Велика корекція** | 16°C (22-10) | За межами +0.5°C |
| 23.2 | +1.2 | ❄️ **Велика корекція** | 16°C (22-10) | >+1.0°C - дуже спекотно |

### Лінійний режим - Приклад: Цільова температура 22°C

| Поточна t°C | Різниця (temp_diff) | Дія | Температура на AC | Розрахунок | Опис |
|-------------|---------------------|-----|-------------------|------------|------|
| 19.5 | -2.5 | ❌ **ВИМКНУТИ** | OFF | - | Занадто холодно |
| 20.9 | -1.1 | ❌ **ВИМКНУТИ** | OFF | - | <-1.0°C - вимикаємо |
| 21.2 | -0.8 | 🔥 **Велика корекція** | 30°C | За межами ±0.5°C | Холодно, велика корекція |
| 21.5 | -0.5 | 📈 Лінійна корекція | 27°C | ratio=0.5/0.5=1.0, corr=+5°C | На межі лінійного діапазону |
| 21.7 | -0.3 | 📈 Лінійна корекція | 25°C | ratio=0.3/0.5=0.6, corr=+3°C | Трохи холодно |
| 22.0 | 0.0 | ✅ Працює | 22°C | Без корекції | Ідеально |
| 22.2 | +0.2 | 📉 Лінійна корекція | 20°C | ratio=0.2/0.5=0.4, corr=-2°C | Трохи тепло |
| 22.5 | +0.5 | 📉 Лінійна корекція | 17°C | ratio=0.5/0.5=1.0, corr=-5°C | На межі лінійного діапазону |
| 22.8 | +0.8 | ❄️ **Велика корекція** | 16°C | За межами ±0.5°C | Тепло, велика корекція |
| 23.2 | +1.2 | ❄️ **Велика корекція** | 16°C | >+1.0°C - мажорна | Дуже спекотно |

### Логіка COOL (Стандартний режим):

```
IF temp_diff < -1.0°C:
    → ВИМКНУТИ кондиціонер (дуже холодно)

ELSE IF temp_diff > +1.0°C:
    → Велика корекція: AC temp = target - 10°C (дуже спекотно)

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    → Велика корекція: AC temp = target ± 10°C (за межами ±0.5°C)

ELSE (-0.5°C ≤ temp_diff ≤ +0.5°C):
    → Працює нормально: AC temp = target (комфортний діапазон)
```

### Логіка COOL (Лінійний режим):

```
IF temp_diff < -1.0°C:
    → ВИМКНУТИ кондиціонер (дуже холодно)

ELSE IF temp_diff > +1.0°C:
    → Велика корекція: AC temp = target - 10°C (дуже спекотно)

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    → Велика корекція: AC temp = target ± 10°C (за межами лінійного діапазону)

ELSE IF temp_diff в діапазоні [-0.5°C, +0.5°C]:
    → Лінійна корекція:
       ratio = |temp_diff| / 0.5°C             # 0.0 до 1.0

       IF temp_diff > 0 (жаркіше):
           correction = ratio × 5°C             # 0°C до 5°C
           AC temp = target - correction        # віднімаємо

       ELSE IF temp_diff < 0 (холодніше):
           correction = ratio × 5°C             # 0°C до 5°C
           AC temp = target + correction        # додаємо

       ELSE (temp_diff = 0):
           AC temp = target                     # без корекції
```

**Діапазон роботи:** від +0.5°C до -1.0°C відносно цілі

---

## 🔄 Перемикання режимів (HEAT ↔ COOL)

При зміні фізичного режиму (наприклад, з HEAT на COOL або навпаки):

- Кондиціонери, які знаходяться в **комфортному діапазоні**, автоматично **вимикаються**
- Це мінімізує непотрібну роботу кондиціонерів при зміні режиму
- Працюють тільки ті кондиціонери, які дійсно потрібні (з великою різницею температур)

**Комфортний діапазон:**
- HEAT: від -0.5°C до +1.0°C
- COOL: від -1.0°C до +0.5°C

---

## 🎯 Практичні приклади

### Приклад 1: Режим COOL, Ціль 22°C

**Ситуація:** Поточна температура 20.6°C
- `temp_diff = 20.6 - 22.0 = -0.6°C`
- Перевірка: `-0.6 < -1.0°C`? → **НІ**
- Перевірка: `-0.6 > +0.5°C`? → **НІ**
- **Результат:** В комфортному діапазоні (-1.0°C до +0.5°C)
- **Дія:** Кондиціонер **працює**, встановлює 22°C
- ✅ Кондиціонер **НЕ вимикається**

### Приклад 2: Режим HEAT, Ціль 22°C

**Ситуація:** Поточна температура 23.2°C
- `temp_diff = 23.2 - 22.0 = +1.2°C`
- Перевірка: `+1.2 > +1.0°C`? → **ТАК**
- **Результат:** Занадто тепло
- **Дія:** Кондиціонер **ВИМИКАЄТЬСЯ**

### Приклад 3: Режим COOL, Ціль 22°C

**Ситуація:** Поточна температура 22.7°C
- `temp_diff = 22.7 - 22.0 = +0.7°C`
- Перевірка: `+0.7 < -1.0°C`? → **НІ**
- Перевірка: `+0.7 > +1.0°C`? → **НІ**
- Перевірка: `+0.7 > +0.5°C`? → **ТАК**
- **Результат:** Трохи спекотно - мала корекція
- **Дія:** Кондиціонер встановлює 17°C (22-5)

---

## ⚙️ Додаткові налаштування

### Інтервали захисту

- **Min mode switch interval** (за замовчуванням 600 сек = 10 хв)
  - Мінімальний час між перемиканнями HEAT ↔ COOL
  - Запобігає частим змінам режиму роботи

- **Min power switch interval** (за замовчуванням 60 сек = 1 хв)
  - Мінімальний час між вмиканням/вимиканням
  - Захищає компресор від частих запусків

### Температурні пороги AUTO режиму

- **Outdoor temp for heat-only** (за замовчуванням 10°C)
  - Нижче цієї температури - тільки обігрів

- **Outdoor temp for cool-only** (за замовчуванням 23°C)
  - Вище цієї температури - тільки охолодження

- **Mode switch temperature threshold** (за замовчуванням 1.5°C)
  - Мінімальне відхилення для перемикання режимів в перехідній зоні

---

## 📝 Формули

### Різниця температури
```
temp_diff = поточна_температура - цільова_температура
```

### Стандартний режим

#### Температура на кондиціонері (HEAT)
```
IF temp_diff < -1.0°C:
    AC_temp = min(target + 10°C, 30°C)  # Велика корекція

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    AC_temp = target ± 10°C             # Велика корекція (за межами ±0.5°C)

ELSE (-0.5°C ≤ temp_diff ≤ +0.5°C):
    AC_temp = target                    # Норма
```

#### Температура на кондиціонері (COOL)
```
IF temp_diff > +1.0°C:
    AC_temp = max(target - 10°C, 16°C)  # Велика корекція

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    AC_temp = target ± 10°C             # Велика корекція (за межами ±0.5°C)

ELSE (-0.5°C ≤ temp_diff ≤ +0.5°C):
    AC_temp = target                    # Норма
```

### Лінійний режим

Лінійна корекція працює **тільки в діапазоні ±0.5°C**. За межами цього діапазону використовується велика корекція ±10°C.

#### Температура на кондиціонері (HEAT)
```
IF temp_diff < -1.0°C:
    AC_temp = min(target + 10°C, 30°C)  # Велика корекція (дуже холодно)

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    AC_temp = target ± 10°C             # Велика корекція (за межами ±0.5°C)

ELSE IF temp_diff в діапазоні [-0.5°C, +0.5°C]:
    ratio = |temp_diff| / 0.5°C         # 0.0 до 1.0
    correction = ratio × 5°C            # 0°C до 5°C

    IF temp_diff < 0:  # холодно
        AC_temp = min(target + correction, 30°C)
    ELSE IF temp_diff > 0:  # тепло
        AC_temp = max(target - correction, 16°C)
    ELSE:
        AC_temp = target  # ідеально
```

#### Температура на кондиціонері (COOL)
```
IF temp_diff > +1.0°C:
    AC_temp = max(target - 10°C, 16°C)  # Велика корекція (дуже жарко)

ELSE IF temp_diff < -0.5°C або temp_diff > +0.5°C:
    AC_temp = target ± 10°C             # Велика корекція (за межами ±0.5°C)

ELSE IF temp_diff в діапазоні [-0.5°C, +0.5°C]:
    ratio = |temp_diff| / 0.5°C         # 0.0 до 1.0
    correction = ratio × 5°C            # 0°C до 5°C

    IF temp_diff > 0:  # жарко
        AC_temp = max(target - correction, 16°C)
    ELSE IF temp_diff < 0:  # холодно
        AC_temp = min(target + correction, 30°C)
    ELSE:
        AC_temp = target  # ідеально
```

### Обмеження температури
- **Мінімум:** 16°C
- **Максимум:** 30°C

---

## 🎨 Візуалізація діапазонів

### HEAT Mode - Стандартний режим
```
    -2°C    -1°C    -0.5°C    0°C    +0.5°C   +1°C    +2°C
     |       |        |        |        |       |       |
     |<-Вел.к>|<-Вел.к>|<-Норма>|<-Вел.к>|<-OFF->|
     |  +10°C | +10°C  |  =t    | -10°C  | Вимк  |
```

### HEAT Mode - Лінійний режим
```
    -2°C    -1°C    -0.5°C    0°C    +0.5°C   +1°C    +2°C
     |       |        |        |        |       |       |
     |<-Вел.к>|<-Вел.к>|<-Лінійна>|<-Вел.к>|<-OFF->|
     |  +10°C | +10°C  | 0→+5→0→-5| -10°C  | Вимк  |
```

### COOL Mode - Стандартний режим
```
    -2°C    -1°C    -0.5°C    0°C    +0.5°C   +1°C    +2°C
     |       |        |        |        |       |       |
     |<-OFF->|<-Вел.к>|<-Норма>|<-Вел.к>|<-Вел.к>|
     | Вімк  | +10°C  |  =t    | -10°C  | -10°C  |
```

### COOL Mode - Лінійний режим
```
    -2°C    -1°C    -0.5°C    0°C    +0.5°C   +1°C    +2°C
     |       |        |        |        |       |       |
     |<-OFF->|<-Вел.к>|<-Лінійна>|<-Вел.к>|<-Вел.к>|
     | Вімк  | +10°C  | 0→-5→0→+5| -10°C  | -10°C  |
```

---

## 🔍 Діагностика

Всі дії логування в Home Assistant:

```
Room Кабінет: temperature control - indoor=20.6, target=22.0, diff=-1.4, mode=heat
Room Кабінет: HEAT mode - major correction: 22.0 + 10.0 = 32.0
Room Кабінет: >>> Calling _control_room_climate with mode=heat, temp=30.0
```

Увімкніть детальне логування:
```yaml
logger:
  default: info
  logs:
    custom_components.smart_climate_controller: debug
```
